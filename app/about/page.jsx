import { AboutClient } from '../../src/next/sitePages';

export const metadata = {
  title: 'About Us',
  description: 'Learn about Project Gazra, its roots, mission, story, and community work.'
};

export default function Page() {
  return <AboutClient />;
}
