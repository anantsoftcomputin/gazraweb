import { AdminEventLocationsClient } from '../../../../src/next/sitePages';

export const metadata = { title: 'Admin Event Locations | Project Gazra', robots: { index: false, follow: false } };

export default function Page() {
  return <AdminEventLocationsClient />;
}
