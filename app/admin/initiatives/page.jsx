import { AdminInitiativesClient } from '../../../src/next/sitePages';

export const metadata = { title: 'Admin Initiatives | Project Gazra', robots: { index: false, follow: false } };

export default function Page() {
  return <AdminInitiativesClient />;
}
