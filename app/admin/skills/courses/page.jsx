import { AdminSkillsCoursesClient } from '../../../../src/next/sitePages';

export const metadata = { title: 'Admin Skill Courses | Project Gazra', robots: { index: false, follow: false } };

export default function Page() {
  return <AdminSkillsCoursesClient />;
}
